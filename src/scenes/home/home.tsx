import Menu from "../../components/menu/menu";

export const Home: React.FC = () => {
    

  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <Menu />
    </div>
  );
}

// You can choose to only have one default export or keep both if needed.
export default Home;
